% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pre-processing.R
\name{pre_process_scrna}
\alias{pre_process_scrna}
\title{Read in and filter data based on mt percentages}
\usage{
pre_process_scrna(
  filepath = NULL,
  file_type = "cellranger",
  filename_pattern = NULL,
  mito_pattern = "^MT-",
  plot_mito = TRUE,
  merge_data = TRUE,
  add_metadata = TRUE,
  metadata = NULL
)
}
\arguments{
\item{filepath}{File path to the directory containing the data}

\item{file_type}{Type of files for input. Either "cellranger", "h5", or "tab"}

\item{filename_pattern}{Regex for the filenames or folders in the filepath. Used if you only want to process
some files or folders e.g. "*h5" for .h5 files.}

\item{mito_pattern}{Gene pattern to recognise mitochondrial genes in the input. Defaults to
"^MT-".}

\item{plot_mito}{Produces violin plots for the mito percentages so you can choose a cutoff}

\item{merge_data}{Should data be merged into a single Seurat object after processing?}

\item{add_metadata}{Should metadata be added to the final object?}

\item{metadata}{Metadata file for samples. This should be formatted as one sample per row, and
information regarding that sample in subsequent columns e.g.
data.frame(sample = c("sample1", "sample2", "sample3", "sample4"),
           disease = c("healthy", "healthy", "disease", "disease"),
           sorted population = c("t_cell", "b_cell", "t_cell", "b_cell"))}
}
\value{
Either a list of Seurat objects of the processed data if merge = FALSE, or a merged Seurat
    object containing processed and filtered data if merge = TRUE.
}
\description{
This function takes an input of cellranger, h5, or tab delim expression
data, filters based on mt percentages, adds metadata to the samples,
and merges all the samples in its output.
}
\examples{
\dontrun{
  data <- pre_process_scrna(filepath = "raw_data",
                            file_type = "cellranger",
                            filename_pattern = "filtered",
                            metadata = "sample_metadata")
}

}
